import React from "react";
import "./style.css";

const Save = (props) => (
    
    <div>
        <button className="save-button" onClick={props.handleSaveButton}>SAVE</button>
    </div>
)

export default Save;